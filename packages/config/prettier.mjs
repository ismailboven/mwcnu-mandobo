/**
 * Shared Prettier configuration.
 * Referensi default: package.json root (`.prettierrc.json`).
 */
const config = {
  semi: true,
  singleQuote: false,
  tabWidth: 2,
  trailingComma: "es5",
  printWidth: 100,
};

export default config;